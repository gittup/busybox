include_rules

lib-@(MKFIFO)	+= getopt_mk_fifo_nod.c
lib-@(MKNOD)	+= getopt_mk_fifo_nod.c
lib-@(INSTALL)	+= cp_mv_stat.c
lib-@(CP)	+= cp_mv_stat.c
lib-@(MV)	+= cp_mv_stat.c

include $(BUSYBOX_ROOT)/busybox.tup
