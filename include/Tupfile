.gitignore
# TODO: shared libs should avoid touch
: ../applets/applet_tables |> %f %o && touch %o |> applet_tables.h NUM_APPLETS.h
: ../applets/usage_compressed | ../applets/usage |> %f %o ../applets && touch %o |> usage_compressed.h
